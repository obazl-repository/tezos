load("@obazl_rules_ocaml//ocaml:rules.bzl", "ocaml_module", "ocaml_ns_archive")

ocaml_module(
    name = "context",
    opts = [
        "-open",
        "Tezos_base__TzPervasives",
        "-open",
        "Tezos_stdlib",
    ],
    struct = ":context.ml",
    deps = ["@tezos//src/lib_base:#Tezos_base"],
)

# okapi:auto
# okapi:public_name tezos-context.sigs
ocaml_ns_archive(
    name = "#Tezos_context_sigs",
    submodules = [":context"],
    visibility = ["//visibility:public"],
)
