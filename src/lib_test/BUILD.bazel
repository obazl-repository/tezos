load("@obazl_rules_ocaml//ocaml:rules.bzl", "ocaml_module", "ocaml_ns_archive", "ocaml_signature")

ocaml_signature(
    name = "qcheck_helpers__sig",
    src = ":qcheck_helpers.mli",
    opts = ["-linkall"],
    deps = [
        "@ocaml//lib/alcotest",
        "@ocaml//lib/alcotest-lwt",
        "@ocaml//lib/qcheck-alcotest",
        "@ocaml//lib/uri",
    ],
)

ocaml_module(
    name = "qcheck_helpers",
    opts = ["-linkall"],
    sig = ":qcheck_helpers__sig",
    struct = ":qcheck_helpers.ml",
    deps = [
        "@ocaml//lib/alcotest",
        "@ocaml//lib/alcotest-lwt",
        "@ocaml//lib/qcheck-alcotest",
        "@ocaml//lib/uri",
    ],
)

# okapi:auto
# okapi:public_name tezos-test-helpers
ocaml_ns_archive(
    name = "#Lib_test",
    submodules = [":qcheck_helpers"],
    visibility = ["//visibility:public"],
)
