load("@obazl_rules_ocaml//dsl:library.bzl", "lib", "mod", "sig")

lib(
    name = "bare-functor-outputs",
    deps_opam = ["lwt"],
    modules = dict(
        hashtbl = mod(),
        map = mod(),
        set = mod(),
    ),
)
